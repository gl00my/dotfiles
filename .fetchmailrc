set daemon 90

poll pop.gmail.com with proto POP3
	user 'TODO' there with password 'TODO' 
	is peter here 
	options ssl keep 
	mda 'procmail -d %T'

poll pop.mail.ru with proto POP3
	user 'TODO' there with password 'TODO'
	is peter here
	options ssl keep
	mda 'procmail -d %T'
