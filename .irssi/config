servers = (
  {
    address = "TODO";
    chatnet = "bitlbee";
    port = "TODO";
    password = "TODO";
    use_ssl = "yes";
    ssl_verify = "no";
    family = "inet";
  }
);

chatnets = {
  bitlbee = { type = "IRC"; username = "TODO/bitlbee"; };
};

settings = {
  "perl/core/scripts" = {
    openurl_app_http = "/usr/local/bin/firefox $1 >/dev/null 2>/dev/null &";
  };
};
